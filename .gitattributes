# Normalise line endings. Without this, editing on Windows rewrites every file
# to CRLF and git reports the whole repo as modified with zero real changes.
* text=auto eol=lf

# Binary — never touch these
*.png binary
*.jpg binary
*.jpeg binary
*.ico binary
*.pdf binary
*.zip binary
